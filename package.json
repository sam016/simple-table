{
  "name": "simple-table",
  "version": "1.0.0",
  "description": "Simple table",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "browser-sync start --ss src -s -f ./src --no-notify --host 0.0.0.0 --port 9000"
  },
  "author": "sam016",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.26.12"
  }
}
